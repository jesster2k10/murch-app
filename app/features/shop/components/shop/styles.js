import { StyleSheet } from 'react-native';
import {
  metrics,
  appStyles,
} from '@styles';

const styles = StyleSheet.create({
  container: {
    ...appStyles.container,
  },
  scrollView: {
    paddingTop: 15,
  },
  list: {
  }
});

export default styles;